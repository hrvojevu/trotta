'use strict';

/**
 * A list of database table names in required seeding order. JS files by the same
 * name should be in the same parent directory.
 */
const tableNames = [
  'user',
  'pool_type',
  'pool',
];

module.exports = tableNames;
